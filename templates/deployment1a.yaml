apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp-green
  namespace: demo-ns
  annotations:
    harness.io/direct-apply: "true" # Marking as non-primary
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: myapp
        image: <+artifact.image>
        ports:
        - containerPort: 8080






