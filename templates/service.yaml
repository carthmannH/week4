apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.name }}
spec:
  type: {{ .Values.serviceType }}
  ports:
   - port: {{ .Values.Port }}
     targetPort: {{ .Values.TargetPort }}
     protocol: TCP
  selector:
    app: myapp
